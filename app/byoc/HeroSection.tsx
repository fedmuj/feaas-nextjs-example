import * as FEAAS from '@sitecore-feaas/clientside/react'
import React from 'react';  
import { Box, Flex, Heading, Text, Button, useColorMode } from '@chakra-ui/react';  
  
const HeroSection = ({ backgroundImage, title, subtitle }) => {  
  const { colorMode } = useColorMode();
  
  return (  
    <Box  
      background={`url(${backgroundImage}) center / cover`}  
      h="400px"  
      w="100%"  
      position="relative"  
    >  
      <Flex  
        h="100%"  
        w="100%"  
        justifyContent="center"  
        alignItems="center"  
        flexDirection="column"  
        bg={colorMode === 'dark' ? "rgba(0, 0, 0, 0.7)" : "rgba(255, 255, 255, 0.7)"}  
      >  
        <Heading as="h1" color={colorMode === 'dark' ? "white" : "black"} fontSize="5xl">  
          {title}  
        </Heading>  
        <Text as="h2" color={colorMode === 'dark' ? "white" : "black"} fontSize="xl">  
          {subtitle}  
        </Text>  
        <Button colorScheme={colorMode === 'dark' ? "whiteAlpha" : "blackAlpha"} mt={4}>
          Know More
        </Button>
      </Flex>  
    </Box>  
  );  
};  
 
FEAAS.registerComponent(HeroSection, {
  name: 'HeroSection',
  title: 'Hero Section',
  description: 'This is a Hero Section in Dark Mode with a Button Generated by AI',
  thumbnail: 'https://mss-p-006-delivery.stylelabs.cloud/api/public/content/3997aaa0d8be4eb789f3b1541bd95c58',
  group: 'AI Generated Components',
  required: ['backgroundImage'],
  properties: {
    backgroundImage: {
      type: 'string',
      title: 'backgroundImage'
    },
    title: {
      type: 'string',
      title: 'title'
    },
    subtitle : {
      type: 'string',
      title: 'title'
    }
  }
})

export default HeroSection;